/*
import 'package:reflectable/reflectable.dart';


class Reflector extends Reflectable {
  const Reflector() : super(
      invokingCapability,
      typingCapability,
      reflectedTypeCapability,
      typeRelationsCapability,
      declarationsCapability,
      typeAnnotationQuantifyCapability,
  );
}*/




/*
var ref = Reflector();

    var typeMir = ref.reflectType(UserDbModel);
    var instanceMir = ref.reflect(UserDbModel);

    for(var i in typeMir.reflectedTypeArguments) {
      pint(i);
    }
    pint('----------');
    for(var i in typeMir.typeArguments) {
      pint(i);
    }
    pint('----------');
    for(var i in typeMir.typeVariables) {
      pint(i);
    }
 */


/*
    VariableMirror variableMirror = classMirror.declarations["english"] as VariableMirror;

  Type type = variableMirror.dynamicReflectedType;
     */